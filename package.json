{
  "name": "todos",
  "version": "0.0.1",
  "description": "Todo Mock REST Service using JSONserver",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node ./node_modules/json-server/bin/index.js -p 9000 -w ./db.json"
  },
  "author": "Jack Stephens",
  "license": "ISC",
  "devDependencies": {
    "json-server": "^0.13.0"
  }
}
